Shared:
1. Landing Page - 
2. Login (student/admin side) - 

Student side:
Dashboard
Profile
Application Form

Admin side:
- Dashboard
- Applicants/Students list
- Application detail

-craete new migration for auth and rbac, just make it simple. two role - students and admin. Create with two seed account. For the existing students records in the database, just let it be. Im thinking of a user can be either a student or a admin, if student it will create a student record in the student table. /grill-me 


admin@example.com
adminpassword

student@example.com
studentpassword

Admin api test
We will login manually to generate a real JWT token.
We will navigate to the Admin Dashboard and verify that the stats display '0' or match our seed data.
We will interact with the Checklist toggles and verify the network tab shows a successful PATCH request, and the UI remains in sync.